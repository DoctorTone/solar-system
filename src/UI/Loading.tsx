import { Html } from "@react-three/drei";
import { PropagateLoader } from "react-spinners";

export const Loading = () => {
  return (
    <Html>
      <PropagateLoader color="#cc7306" />
    </Html>
  );
};
